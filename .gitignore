node_modules
.next
.turbo
.contentlayer
.source
.fumadocs
.DS_Store
.env.local
*.log
.cache
dist
out
.swc
.vercel
.idea
.vscode
# themes/ and examples/ are committed as snapshots from
# ../freedom-ui-kit/{themes,examples}. scripts/sync-{themes,examples}.mjs
# refresh them during local dev when the kit repo is adjacent; on
# Vercel / CI the committed copies are used as-is.
# preview app: vite cache + build output
preview/.vite
preview/dist
