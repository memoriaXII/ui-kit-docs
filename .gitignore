node_modules
.next
.turbo
.contentlayer
.source
.fumadocs
.DS_Store
.env.local
*.log
.cache
dist
out
.swc
.vercel
.idea
.vscode
# themes/ is committed as a snapshot from ../freedom-ui-kit/themes.
# scripts/sync-themes.mjs refreshes it during local dev when the kit
# repo is adjacent; on Vercel / CI the committed copy is used as-is.
# preview app: vite cache + build output
preview/.vite
preview/dist
