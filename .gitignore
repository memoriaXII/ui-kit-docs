node_modules
.next
.turbo
.contentlayer
.source
.fumadocs
.DS_Store
.env.local
*.log
.cache
dist
out
.swc
.vercel
.idea
.vscode
# themes/ is synced from ../freedom-ui-kit/themes at predev. The local
# copy is treated as a build artifact -- if you've cloned this repo
# standalone (no kit adjacent), drop your own themes/<brand>/theme.css
# in place and remove this line.
/themes
# preview app: vite cache + build output
preview/.vite
preview/dist
