---
title: Components
description: All Freedom UI Kit components.
---

This is a placeholder index page for the components catalog. Add one MDX file per component in this folder and reference it in `meta.json`.

Each component page should include at least one `<ComponentPreview name="..." />` block that points at an entry registered in `registry/__index__.tsx`.
