---
title: Markdown
description: Render markdown strings using the kit's typography so headings, lists and paragraphs stay on-brand.
---

## Default

Headings map to `Title*`, paragraphs to `Body1`, list items to `Body2`. Backed by `react-markdown`.

<ComponentPreview name="markdown-default" />

## CMS / i18n content

Use this when copy comes from a CMS or translation catalog and you don't want to hand-render JSX. Pass `componentClass` to fine-tune individual elements.

<ComponentPreview name="markdown-cms" />

## Installation

```bash
pnpm add @appboxo/ui-kit
```
