---
title: Flex
description: Thin Flexbox primitive — vertical / gap / align / justify shortcuts that the rest of the kit composes with.
---

## Vertical stack

`vertical` + `gap`.

<ComponentPreview name="flex-vertical" />

## Horizontal row

Default direction. `wrap="wrap"` for responsive overflow.

<ComponentPreview name="flex-horizontal" />

## Space between

`justify="space-between"` + `align="center"` for label/value rows.

<ComponentPreview name="flex-between" />

## Centered

Combine `align="center"` + `justify="center"` for empty states.

<ComponentPreview name="flex-centered" />

## Installation

```bash
pnpm add @appboxo/ui-kit
```
