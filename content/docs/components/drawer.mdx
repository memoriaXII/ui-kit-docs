---
title: Drawer
description: Bottom sheet for confirmations, pickers and contextual flows. Wraps Arco's PopupSwiper.
---

## Confirmation

The standard pattern. Default drag-handle + swipe-down to dismiss.

<ComponentPreview name="drawer-confirm" />

## Details

Use for read-only flows with mixed content (key-value pairs, lists, CTAs).

<ComponentPreview name="drawer-details" />

## Without drag handle

Pass `hideControl` to hide the grab indicator at the top. Use when you want users to dismiss only via the explicit close button.

<ComponentPreview name="drawer-no-handle" />

## Installation

```bash
pnpm add @appboxo/ui-kit
```
