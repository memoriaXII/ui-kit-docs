---
title: TouchCell
description: Tappable row with proper mobile touch feedback. Use for list items, settings, picker triggers.
---

## Plain rows

`label` accepts a string for terse settings lists.

<ComponentPreview name="touch-cell-plain" />

## Label + description

Use `desc` for a secondary line.

<ComponentPreview name="touch-cell-desc" />

## Trailing value

Pass JSX into `label` to render a label / value pair on the same row.

<ComponentPreview name="touch-cell-trailing" />

## Installation

```bash
pnpm add @appboxo/ui-kit
```

## Theme tokens

`--cell-*` (Arco-managed).
