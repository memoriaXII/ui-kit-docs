---
title: PickerInput
description: Touch-friendly wheel picker bound to a kit-styled trigger input.
---

## Default

Tap the input to open the wheel picker. Reads `useUIKitTranslation()` for the "Done" / "Close" labels.

<ComponentPreview name="picker-input-default" />

## Error state

`hasError` mirrors `Input` — surfaces validation on the trigger.

<ComponentPreview name="picker-input-error" />

## Installation

```bash
pnpm add @appboxo/ui-kit
```

## Requires UIKitProvider

PickerInput reads translations from `useUIKitTranslation()`. Wrap in `<NextUIKitProvider>` (Next) or `<UIKitProvider translation={useTranslation}>` (Vite / CRA).

See [DatePicker](/docs/components/date-picker) and [TimePicker](/docs/components/time-picker) for date-aware specializations built on the same primitive.
