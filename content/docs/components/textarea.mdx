---
title: TextArea
description: Multi-line text input (note the capital A).
---

## Default

Empty state with `autoSize` between 3 and 8 rows.

<ComponentPreview name="textarea-default" />

## Filled

Pre-filled content. `autoSize` grows the box up to the cap as the user types.

<ComponentPreview name="textarea-filled" />

## Error

`hasError` mirrors `Input`.

<ComponentPreview name="textarea-error" />

## Disabled

Greyed out + non-interactive.

<ComponentPreview name="textarea-disabled" />

## Installation

```bash
pnpm add @appboxo/ui-kit
```
