---
title: LayoutLoading
description: Full-screen loading state for the initial render of a route.
---

## Usage

```tsx
import { LayoutLoading } from "@appboxo/ui-kit"

export function Example() {
  return <LayoutLoading />
}
```

Use as the fallback for Suspense boundaries or as the rendered output while a route's primary data is loading.
