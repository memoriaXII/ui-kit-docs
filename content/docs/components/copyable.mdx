---
title: Copyable
description: Inline text that copies to clipboard on tap, with optional haptic feedback.
---

## Inline reference

Wrap any text — clicking copies the value to clipboard. If a haptic handler is registered via `setHapticHandler`, a `"light"` impact fires on success.

<ComponentPreview name="copyable-inline" />

## Long string

Useful for opaque tokens / signed URLs / long IDs.

<ComponentPreview name="copyable-long" />

## Installation

```bash
pnpm add @appboxo/ui-kit
```
